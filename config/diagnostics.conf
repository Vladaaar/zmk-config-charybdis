# Optional diagnostic build only: append with -DEXTRA_CONF_FILE=...
# Use the zmk-usb-logging snippet; disable Studio for this diagnostic build.
CONFIG_ZMK_USB_LOGGING=y
CONFIG_ZMK_LOG_LEVEL_DBG=y
CONFIG_STACK_SENTINEL=y
CONFIG_THREAD_STACK_INFO=y
